# CloudFront CDN domain for serving recipe images
# Automatically read from amplify_outputs.json custom.CloudFrontDomain
# when deployed via `npx ampx sandbox deploy`.
# Set manually only as a fallback override.
# VITE_CLOUDFRONT_DOMAIN=d1q9k2f3g4h5i6.cloudfront.net
